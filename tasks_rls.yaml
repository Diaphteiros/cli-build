version: 3

includes:
  build:
    taskfile: tasks_build.yaml
    internal: true

tasks:
  # This is a dummy task that serves as a separator between task namespaces in the 'task -l' output.
  "---":
    desc: "{{.RLS_SEP}}"
    cmds:
    - cmd: echo "{{.SEP_MSG}}"
      silent: true

  patch:
    desc: "  Prepares a new patch release by creating a commit that will result in a patch release when pushed."
    run: once
    requires:
      vars:
      - VERSION
    cmds:
    - task: release-internal
      vars:
        RELEASE_VERSION:
          sh: 'VERSION={{.VERSION}} "{{.TASKFILE_DIR2}}/compute-next-release-version.sh" patch'

  minor:
    desc: "  Prepares a new minor release by creating a commit that will result in a minor release when pushed."
    run: once
    requires:
      vars:
      - VERSION
    cmds:
    - task: release-internal
      vars:
        RELEASE_VERSION:
          sh: 'VERSION={{.VERSION}} "{{.TASKFILE_DIR2}}/compute-next-release-version.sh" minor'

  major:
    desc: "  Prepares a new major release by creating a commit that will result in a major release when pushed."
    run: once
    requires:
      vars:
      - VERSION
    cmds:
    - task: release-internal
      vars:
        RELEASE_VERSION:
          sh: 'VERSION={{.VERSION}} "{{.TASKFILE_DIR2}}/compute-next-release-version.sh" major'

  prepare-dev-cycle:
    desc: "  Prepares the repository for further development after a release. Creates a commit that adds a '-dev' suffix to the version and re-adds any commented-out 'replace' directives for nested modules in the root go.mod file. Meant to be run after a release commit has been pushed to the main branch."
    run: once
    requires:
      vars:
      - VERSION
      - MODULE_NAME
      - NESTED_MODULES
    cmds:
    - task: set-version
      vars:
        CLI_ARGS: '{{.VERSION}}-dev'
    - '( cd "{{.ROOT_DIR2}}"; git add pkg/version go.mod; git commit -m "chore(release): prepare for next development iteration (version {{.VERSION}}-dev)" )'

  prompt-changes:
    desc: "  Causes a prompt if there are uncommitted changes which would be included in the release commit."
    run: once
    vars:
      changes:
        sh: '( cd "{{.ROOT_DIR2}}"; git status --short )'
    if: 'test -n "$( cd "{{.ROOT_DIR2}}" && git status --porcelain=v1)"'
    prompt:
    - 'There are uncommitted changes in the working directory:{{"\n"}}{{.changes}}{{"\n\n"}}These changes will be included in the release commit, unless you stash, commit, or remove them otherwise before. Do you want to continue?'
    internal: true
  
  release-internal:
    desc: "  Prepares a new release by creating a commit that will result in a release when pushed."
    run: once
    deps:
    - prompt-changes
    requires:
      vars:
      - RELEASE_VERSION
      - MODULE_NAME
      - NESTED_MODULES
    prompt:
    - 'The release version will be {{.RELEASE_VERSION}}. Do you want to continue?'
    cmds:
    - cmd: 'echo "Creating release commit for version {{.RELEASE_VERSION}} ..."'
      silent: true
    - vars:
        CLI_ARGS: '{{.RELEASE_VERSION}}'
      task: set-version
    - '( cd "{{.ROOT_DIR2}}"; git add --all; git commit -m "feat: release {{.RELEASE_VERSION}}" )'
    internal: true

  set-version:
    desc: '  Updates all versions to the specified version. Usage: task {{.TASK}} -- <version>'
    summary: |
      This task is called by the release tasks automatically.
      It sets the following versions to the one provided as argument:
      - the global CLI version in pkg/version/static_version.go
      - the imported version of each nested module in the root go.mod file
    run: once
    requires:
      vars:
      - MODULE_NAME
    preconditions:
    - sh: 'test "{{len .CLI_ARGS}}" -gt 0'
      msg: 'This task requires a version argument. Usage: task {{.TASK}} -- <version>'
    vars:
      VERSION: '{{.CLI_ARGS | splitList " " | first}}'
    cmds:
    - '{{.TASKFILE_DIR2}}/set-root-version.sh "{{.VERSION}}"'
    - if: '[[ ! "{{.VERSION}}" =~ .*-dev(-[0-9a-f]*)? ]]' # only set nested module versions for release versions (without '-dev' suffix)
      for:
        var: NESTED_MODULES
      vars:
        MODULE: '{{.ITEM}}'
        VERSION: '{{.VERSION}}'
        MODULE_NAME: '{{.MODULE_NAME}}'
      task: set-module-version-internal

  set-module-version-internal:
    desc: "  Sets the imported version of a nested module in the root go.mod file."
    run: always
    requires:
      vars:
      - VERSION
      - MODULE
      - MODULE_NAME
    cmds:
    - '{{.TASKFILE_DIR2}}/sed.sh -E "s@	{{.MODULE_NAME}}/{{.MODULE}} v.*@	{{.MODULE_NAME}}/{{.MODULE}} {{.VERSION}}@" "{{.ROOT_DIR2}}/go.mod"'
    - for:
        var: NESTED_MODULES
      vars:
        MODULE_TO_UPDATE: '{{.MODULE}}'
        MODULE_DEPENDENCY: '{{.ITEM}}'
        VERSION: '{{.VERSION}}'
        MODULE_NAME: '{{.MODULE_NAME}}'
      task: set-nested-module-version-internal
    internal: true

  set-nested-module-version-internal:
    desc:  "  Sets the imported version of a nested module in a nested go.mod file."
    run: always
    requires:
      vars:
      - MODULE_TO_UPDATE
      - MODULE_DEPENDENCY
      - VERSION
      - MODULE_NAME
    cmds:
    - '{{.TASKFILE_DIR2}}/sed.sh -E "s@	{{.MODULE_NAME}}/{{.MODULE_DEPENDENCY}} v.*@	{{.MODULE_NAME}}/{{.MODULE_DEPENDENCY}} {{.VERSION}}@" "{{.ROOT_DIR2}}/{{.MODULE_TO_UPDATE}}/go.mod"'
    internal: true

  list-nested-modules:
    desc: "List all nested modules in the project."
    cmds:
      - cmd: 'echo "{{.NESTED_MODULES}}"'

  generate-changelog:
    desc: "  Generates an automated release changelog based on conventional-commits PR titles in CHANGELOG.md file."
    run: once
    requires:
      vars:
      - VERSION
    cmds:
    -  'VERSION={{.VERSION}} {{.TASKFILE_DIR2}}/generate-changelog.sh'

  artifacts:
    desc: "  Creates the artifacts to be attached to a github release. Basically tar archives of the built binaries for each platform. Outputs them to bin/release."
    run: once
    requires:
      vars:
      - NAME
      - VERSION
    vars:
      VERSION_NO_PREFIX: '{{.VERSION | replaceRE "^[vV]" ""}}'
    deps:
    - build:bin-multi-raw
    cmds:
    - 'rm -rf {{.ROOT_DIR2}}/bin/release && mkdir -p {{.ROOT_DIR2}}/bin/release'
    - for:
        matrix:
          OS:
            ref: .ALL_OS
          ARCH:
            ref: .ALL_ARCH
      cmd: 'rm -f "{{.ROOT_DIR2}}/bin/{{.NAME}}" && mv "{{.ROOT_DIR2}}/bin/{{.NAME}}.{{.ITEM.OS}}-{{.ITEM.ARCH}}" "{{.ROOT_DIR2}}/bin/{{.NAME}}" && tar -czf "{{.ROOT_DIR2}}/bin/release/{{.NAME}}-{{.VERSION_NO_PREFIX}}-{{.ITEM.OS}}-{{.ITEM.ARCH}}.tar.gz" -C "{{.ROOT_DIR2}}/bin" {{.NAME}}'

